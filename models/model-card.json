{
  "model_name": "MobileViT-XXS-Quantized-INT8",
  "architecture": "MobileViT (Visual Transformer + MobileNetV2 Block)",
  "purpose": "On-Device Visual Perception & Sensitive PII Bounding Detection",
  "task": "sih26171-visual-pii-detection",
  "implementation_status": "PROTOTYPE PLACEHOLDER (Spatial bounding & DOM heuristic fallback; ONNX weights pending future bundling)",
  "bundled_weights_present": false,
  "runtime_frameworks": [
    "ONNX Runtime Web (WebGPU / WASM)",
    "Transformers.js (Hugging Face)"
  ],
  "input_shape": [1, 3, 224, 224],
  "weights_format": "ONNX (Quantized INT8)",
  "memory_footprint_mb": 4.8,
  "execution_providers": {
    "primary": "WebGPU (Direct hardware acceleration via Chrome navigator.gpu)",
    "fallback": "WASM (WebAssembly multi-threaded CPU fallback)"
  },
  "benchmarks": {
    "webgpu_latency_ms": "8 - 15ms",
    "wasm_cpu_latency_ms": "28 - 45ms",
    "ram_peak_mb": "18MB"
  },
  "supported_entity_categories": [
    "PASSWORD",
    "PAYMENT_CARD",
    "GOV_ID_SSN",
    "GOV_ID_AADHAAR",
    "GOV_ID_PAN",
    "EMAIL",
    "PHONE_NUMBER",
    "FACE_AVATAR",
    "FINANCIAL_INFO"
  ],
  "byom_compatibility": {
    "huggingface_compatible": true,
    "onnx_compatible": true,
    "remote_endpoint_compatible": true,
    "example_hf_models": [
      "Xenova/mobilevit-small",
      "Xenova/vit-base-patch16-224"
    ]
  }
}
